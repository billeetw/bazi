# 🚀 启动测试服务器

## 方法 1: 使用 Python（最简单）

在终端运行：

```bash
cd /Users/bill/bazi-project
python3 -m http.server 8000
```

然后在浏览器打开：
- **主页面**: http://localhost:8000/index.html
- **UI 测试**: http://localhost:8000/test-ui-split.html

## 方法 2: 使用 Node.js

在终端运行：

```bash
cd /Users/bill/bazi-project
node start-local-server.js
```

## 方法 3: 使用 VS Code Live Server（如果已安装）

1. 右键点击 `index.html`
2. 选择 "Open with Live Server"

## 方法 4: 直接打开文件（可能有 CORS 限制）

如果只是想快速查看 UI，可以直接双击：
- `index.html` 
- `test-ui-split.html`

⚠️ 注意：直接打开文件可能会有 CORS 限制，某些功能可能无法正常工作。

## 🔧 如果端口被占用

尝试其他端口：

```bash
# Python
python3 -m http.server 3000

# Node.js
node start-local-server.js 3000
```

然后访问 `http://localhost:3000/index.html`

## 📝 快速测试命令

复制粘贴到终端：

```bash
cd /Users/bill/bazi-project && python3 -m http.server 8000
```

按 `Ctrl+C` 停止服务器。
