{
  "name": "bazi-api-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:canonical-md": "WRITE_CANONICAL_DOC=1 vitest run tests/lifebookCanonicalChart.test.ts"
  },
  "dependencies": {
    "iztro": "^2.5.0",
    "lunar-lite": "^0.2.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.73.0"
  }
}
