# 紫微戰略圖：九層演算法架構 (The 9-Layer Architecture)

> **系統靈魂**：這是整個紫微評分系統的核心架構，所有代碼實現都必須遵循這個資料流向。

---

## 🏗️ 架構總覽

```
L1-L3 (核心硬體) → L4 (空間連動) → L5 (能量環境) → L6-L7 (觸發與感知) → L8 (細節噪訊) → L9 (戰略輸出)
```

---

## 📐 各層詳細說明

### **L1-L3: 核心硬體 (Core Hardware)**

#### **L1: 主星基礎分數**
- **職責**: 計算主星的基礎權重分數（`baseScore`）
- **實現**: `stageBaseScore` 中處理 `category === 'main'` 的星曜
- **資料流**: `starCtx.baseScore = config.baseScore`

#### **L2: 輔星基礎分數**
- **職責**: 計算輔星的基礎權重分數（`baseScore`）
- **實現**: `stageBaseScore` 中處理 `category === 'assistant'` 的星曜
- **資料流**: `starCtx.baseScore = config.baseScore`

#### **L3: 廟旺利陷與宮位共鳴修正**
- **職責**: 
  - 根據星曜亮度狀態（廟/旺/利/陷）應用乘數
  - 根據星曜與宮位的共鳴度應用係數
- **實現**: 
  - `stageBrightness`: 應用 `brightnessMultiplier[status]`
  - `stageResonance`: 應用 `resonanceMap[palaceId]`
- **資料流**: 
  ```
  baseScore *= brightnessMultiplier[status]
  baseScore *= resonanceMap[palaceId]
  ```

**當前狀態**: ✅ **已完成**

---

### **L4: 空間連動 (Spatial Interconnection)**

- **職責**: 實作三方四正的加權計算
- **權重分配**:
  - 本宮 (Current Palace): 100%
  - 對宮 (Opposite Palace): 40%
  - 三合宮位 (Two Triad Palaces): 各 20%
- **公式**: 
  ```
  FinalScore = Score_Self (100%) + (Score_Opposite * 0.4) + (Score_Triad1 * 0.2) + (Score_Triad2 * 0.2)
  ```
- **實現**: `computePalaceBaseScore` 函數
- **資料流**: 
  ```javascript
  const selfResult = computeSinglePalaceScore(ziwei, palaceName, ...);
  const oppositeResult = computeSinglePalaceScore(ziwei, opposite, ...);
  const triad1Result = computeSinglePalaceScore(ziwei, triads[0], ...);
  const triad2Result = computeSinglePalaceScore(ziwei, triads[1], ...);
  const finalScore = selfResult.score + (oppositeResult.score * 0.4) + ...
  ```

**當前狀態**: ✅ **已完成**

---

### **L5: 能量環境 (Energy Environment)**

- **職責**: 透過五行生剋調整能效比（Efficiency Ratio）
- **邏輯**: 
  - 根據宮位五行、星曜五行計算相生相剋關係
  - 應用能效比乘數
- **實現**: `stageElement` 函數（目前為預留擴展接口）
- **資料流**: 
  ```javascript
  efficiencyRatio = calculateElementEfficiency(starElement, palaceElement);
  baseScore *= efficiencyRatio;
  ```

**當前狀態**: ⚠️ **預留接口，待實作**

---

### **L6-L7: 觸發與感知 (Trigger & Perception)**

#### **L6: 四化 (SiHua) 權重調整**
- **職責**: 處理化祿、化權、化科、化忌的權重調整
- **權重**: 
  - 化祿 (Lu): +3
  - 化權 (Quan): +2
  - 化科 (Ke): +1
  - 化忌 (Ji): -3
- **實現**: `stageSiHua` 函數
- **資料流**: 
  ```javascript
  sihuaBoost = sum(sihuaWeights[hua] for each hua in palace);
  baseScore += sihuaBoost;
  ```

#### **L7: 個人小限 (XiaoXian) 權重放大**
- **職責**: 根據小限天干動態調整四化權重
- **實現**: `getPalaceScoreWithWeights` 函數
- **資料流**: 
  ```javascript
  const weights = getSiHuaWeights(stem); // 根據小限天干獲取四化權重
  finalScore = baseScore + sum(weights[starName] for each star);
  ```

**當前狀態**: ✅ **已完成**

---

### **L8: 細節噪訊 (Detail Noise)**

- **職責**: 處理雜曜與神煞的微調與特殊懲罰規則
- **分類**:
  - **雜曜 (Minor Stars)**: 37 顆，增壓範圍 ±10 分
  - **神煞 (Deities)**: 48 顆，觸發特殊機制
    - `penaltyTrigger`: 特定宮位額外懲罰
    - `maxStarRating`: 星等上限鎖定
    - `strategicTag`: 戰略建議標籤
    - `penaltyTrigger2026`: 2026 預警機制
- **實現**: `stagePenalty` 函數
- **資料流**: 
  ```javascript
  minorBoost = sum(minorStar scores) // 限制在 ±10
  penaltyApplied = sum(deity penalties)
  maxStarRating = min(deity maxStarRatings)
  strategicAdvice = collect(deity strategicTags)
  ```

**當前狀態**: ✅ **已完成**

---

### **L9: 戰略輸出 (Strategic Output)**

- **職責**: 將最終得分轉化為 1-5 顆星與「#深度貼文」風格的戰略文字
- **轉換邏輯**:
  1. 標準化分數（0-100）
  2. 計算星等（1-5 顆星）
  3. 應用星等上限限制
  4. 生成戰略文字（#深度貼文風格）
- **實現**: 需要創建 `finalizeStarRating` 函數
- **資料流**: 
  ```javascript
  normalizedScore = normalize(finalScore, 0-100);
  starRating = calculateStarRating(normalizedScore, maxScore);
  starRating = applyMaxStarRatingLimit(starRating, maxStarRating);
  strategicText = generateStrategicText(starRating, strategicAdvice, palaceName);
  ```

**當前狀態**: ⚠️ **部分完成（星等計算在 UI 層），需要整合到 L9**

---

## 🔄 資料流向圖

```
┌─────────────────────────────────────────────────────────────┐
│                    L1-L3: 核心硬體                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │ L1: 主星 │→ │ L2: 輔星 │→ │ L3: 亮度 │→ │ L3: 共鳴 │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    L4: 空間連動                             │
│  本宮(100%) + 對宮(40%) + 三合1(20%) + 三合2(20%)          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    L5: 能量環境                             │
│              五行生剋能效比調整（預留）                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  L6-L7: 觸發與感知                          │
│  ┌──────────┐  ┌──────────┐                                │
│  │ L6: 四化 │→ │ L7: 小限 │                                │
│  └──────────┘  └──────────┘                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    L8: 細節噪訊                             │
│  雜曜增壓(±10) + 神煞懲罰 + 星等上限 + 戰略建議             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    L9: 戰略輸出                             │
│  標準化 → 星等(1-5) → 戰略文字(#深度貼文風格)               │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 當前實現對照表

| 層級 | 名稱 | 實現函數 | 狀態 | 備註 |
|------|------|----------|------|------|
| L1 | 主星基礎分數 | `stageBaseScore` | ✅ | 處理 `category === 'main'` |
| L2 | 輔星基礎分數 | `stageBaseScore` | ✅ | 處理 `category === 'assistant'` |
| L3 | 亮度與共鳴 | `stageBrightness`, `stageResonance` | ✅ | 廟旺利陷 + 宮位共鳴 |
| L4 | 三方四正加權 | `computePalaceBaseScore` | ✅ | 本宮100% + 對宮40% + 三合各20% |
| L5 | 五行能效比 | `stageElement` | ⚠️ | 預留接口，待實作 |
| L6 | 四化權重 | `stageSiHua` | ✅ | 化祿+3, 化權+2, 化科+1, 化忌-3 |
| L7 | 小限權重 | `getPalaceScoreWithWeights` | ✅ | 根據小限天干動態調整 |
| L8 | 雜曜神煞 | `stagePenalty` | ✅ | 雜曜±10, 神煞特殊機制 |
| L9 | 戰略輸出 | UI 層 `renderZiweiScores` | ⚠️ | 需要整合到 L9 函數 |

---

## 🎯 實作原則

### 1. **模組化 (Modular)**
- 每一層都是獨立的函數/模組
- 可以單獨測試和維護
- 職責清晰，不互相干擾

### 2. **高可擴展性 (High Extensibility)**
- 添加新功能只需在對應層級擴展
- 不影響其他層級的邏輯
- 預留擴展接口（如 L5）

### 3. **資料流向清晰**
- 從 L1-L3 → L4 → L5 → L6-L7 → L8 → L9
- 每一層的輸出作為下一層的輸入
- 不允許跨層級直接訪問

### 4. **向後兼容**
- 保持現有 API 不變
- 內部重構不影響外部調用
- 逐步遷移，不破壞現有功能

---

## 🚀 下一步實作計劃

1. **完善 L5 (五行能效比)**
   - 實作五行生剋計算邏輯
   - 整合到 `stageElement` 函數

2. **整合 L9 (戰略輸出)**
   - 創建 `finalizeStarRating` 函數
   - 實現「#深度貼文」風格文字生成
   - 將 UI 層的星等計算邏輯遷移到 L9

3. **優化資料流**
   - 確保每一層的輸入輸出格式統一
   - 添加類型檢查和錯誤處理

4. **文檔完善**
   - 為每個層級添加詳細註釋
   - 創建使用範例

---

## 📚 相關文件

- `js/calc.js`: 主要實現文件
- `REFACTORING_PIPELINE.md`: Pipeline 架構說明
- `PIPELINE_REFACTORING_COMPLETE.md`: 重構完成報告
- `data/ziweiWeights.json`: 權重配置數據

---

**最後更新**: 2026-02-04  
**架構版本**: 1.0
